log-triage has been deployed as "{{ include "log-triage.fullname" . }}" in namespace {{ .Release.Namespace }}.

1. Check rollout status:
   kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "log-triage.fullname" . }}

2. Port-forward to reach the API locally:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "log-triage.fullname" . }} 8000:{{ .Values.service.port }}

3. Verify health:
   curl localhost:8000/healthz
   curl localhost:8000/readyz

4. List templates awaiting categorization:
   curl "localhost:8000/api/templates?status=pending"

5. Categorize a template (severity: error|warning|info):
   curl -X POST localhost:8000/api/templates/<cluster_id>/categorize \
     -H "Content-Type: application/json" \
     -d '{"severity": "error", "muted": false}'

6. Check metrics:
   curl localhost:8000/metrics | grep logtriage_logs_total

{{- if .Values.serviceMonitor.enabled }}

A ServiceMonitor was created — confirm it's picked up by your Prometheus Operator's
serviceMonitorSelector (values.serviceMonitor.labels must match), then check the
Targets page in Prometheus for {{ include "log-triage.fullname" . }}.
{{- else }}

serviceMonitor.enabled is false — no ServiceMonitor was created. Set it to true if
your cluster runs the Prometheus Operator / kube-prometheus-stack, or scrape
{{ include "log-triage.fullname" . }}:{{ .Values.service.port }}/metrics directly otherwise.
{{- end }}

See charts/log-triage/README.md for full installation and upgrade notes.
